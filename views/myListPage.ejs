<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>myListPage</title>
<%- include('header') -%>
<main>


    <% if(data.length > 0){ %>
        <%data.map(ele=>{%>
            <div>
                <div class='content'>
                    <p><%=ele.title%></p>
                    <p><%=ele.company%></p>
                    <p><%=ele.location%></p>
                    <a href="<%=ele.url%>"><%=ele.url%></a>
                    <form action="\jobDetailsPage\<%=ele.id%>" method="get">
                      
                      <input type="submit" value="View Details">
                    </form>
                </div>
            </div>
            
            <%});%>
    <% } else{ %>  
        <p>No Jobs in Your List</p>
    <% } %>


  
</main>
    <%- include('footer') -%>
</body>
</html>